/** Bumped on each scanner ship so the live UI proves which build is deployed. */
export const BOUNCE_BUILD = "eagles-jumpers";
export const BOUNCE_BUILD_NOTE =
  "West Coast jumper backfill fixed (westcoast team id resolves). Stamp must read eagles-jumpers.";
